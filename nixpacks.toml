[variables]
NODE_VERSION = "18"

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci --prefix backend"]

[phases.build] 
cmds = ["npm run build --prefix backend"]

[phases.start]
cmd = "npm run db:migrate --prefix backend && npm start --prefix backend"