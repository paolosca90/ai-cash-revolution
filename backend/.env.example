# Environment Variables for AI Trading Bot Backend

# Application Configuration
NODE_ENV=development
PORT=3001

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# MT5 Configuration (Optional)
MT5_SERVER_HOST=localhost
MT5_SERVER_PORT=8080

# Trading Configuration
DEFAULT_RISK_PERCENTAGE=2.0
DEFAULT_ACCOUNT_BALANCE=10000

# Note: Copy this to .env and replace with your actual values
# Never commit the actual .env file with real credentials to version control!
