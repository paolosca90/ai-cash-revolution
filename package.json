{
  "name": "leap-app",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "deploy": "cd frontend && npm run build && gh pr create --title 'Deploy to production' --body 'Production deployment with optimized build' --fill || git push origin main"
  }
}