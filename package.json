{
  "name": "leap-app",
  "version": "1.0.0",
  "type": "commonjs",
  "packageManager": "bun",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "cd backend && encore run",
    "build": "cd backend && bun run build",
    "test": "cd backend && encore test",
    "deploy": "cd backend && encore deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "encore.dev": "^1.49.1"
  }
}